using Kindred.Domain.Entities;

namespace Kindred.Infrastructure.Service
{
    public class UserService
    {
        /*private User? _user;

        public User GetUser()
        {
            if (_user == null)
            {
                _user = User.GetInstance();
            }
            return _user;
        }

        public void SetUser(string userName, string email)
        {
            var user = User.GetInstance();
            user.UserName = userName;
            user.Email = email;
            _user = user;
        }*/
    }


}
