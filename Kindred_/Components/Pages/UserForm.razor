@page "/user"
@* @inject UserService UserService *@

<PageTitle>User</PageTitle>

<h3>На даній сторінці повинно було б відображатись створення Користувача</h3>

Реалізація патерну Singleton
<br/>
Демонстрація коду

@* <EditForm FormName="AddUserForm" Model="@user" OnValidSubmit="@HandleValidSubmit">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <div class="mb-3">
        <label for="userName" class="form-label">Ім'я користувача</label>
        <InputText id="userName" class="form-control" @bind-Value="user.UserName" />
    </div>

    <div class="mb-3">
        <label for="email" class="form-label">Електронна пошта</label>
        <InputText id="email" class="form-control" @bind-Value="user.Email" />
    </div>

    <button type="submit" class="btn btn-primary">Зберегти</button>
</EditForm>

@code {
    private User user = new User();

    private void HandleValidSubmit()
    {
        UserService.SetUser(user.UserName, user.Email);
        // Логіка після успішного збереження користувача
    }
} *@
